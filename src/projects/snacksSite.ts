import type { Project } from '../types'

const description = `A website for my band, Nasty Snacks. Built using Astro and
Sanity, integrated with a Discord bot so we can update the site via slash
commands`

export const snacksSite: Project = {
  projectName: 'Nasty Snacks',
  projectDescription: description,
  projectLink: `https://nastysnacks.com`,
}
